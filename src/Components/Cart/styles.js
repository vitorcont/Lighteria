
import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    cart: {
        backgroundColor: '#fff',
        padding: 18,
        borderRadius: 30,
    },
    image: {
        height: 30,
        width: 30,
    },
})

export default styles