import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    botaoContainer: {
        backgroundColor: "#889fb8",
        padding: 20,
        borderRadius: 10,
    },
    botaoTexto: {
        fontFamily: 'OpenSans',
        color: '#fff',
        fontWeight: "700"
    },
})

export default styles